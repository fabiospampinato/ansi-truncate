
/* MAIN */

// The following isn't perfect, but it's probably good enough

const getWidth = ( str: string ): number => {

  return Array.from ( str ).length;

};

/* EXPORT */

export default getWidth;
